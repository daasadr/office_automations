# Frontend - Environment Configuration
# Copy this file to .env and update with your values
# Command: cp env.template .env

# ============================================================================
# INTERNAL SERVICE URLS (Server-Side)
# ============================================================================
# These URLs are used by the frontend server to communicate with backend services
# Use Docker service names when running in containers
DIRECTUS_URL=http://directus:8055
ORCHESTRATION_API_URL=http://orchestration-api:3001

# For local development (not using Docker), use:
# DIRECTUS_URL=http://localhost:8055
# ORCHESTRATION_API_URL=http://localhost:3001

# ============================================================================
# PUBLIC URLS (Client-Side)
# ============================================================================
# These URLs are exposed to the browser
# Should match your domain configuration
PUBLIC_DOMAIN=dev-dejtoai.local  # or dejtoai.cz for production
PUBLIC_DIRECTUS_URL=https://${PUBLIC_DOMAIN}/admin
PUBLIC_API_URL=https://${PUBLIC_DOMAIN}/api

# For local development (not using Docker):
# PUBLIC_DOMAIN=localhost:4321
# PUBLIC_APP_URL=http://localhost:4321

# Base path for the frontend (e.g., "/app" or leave empty for root)
# This will be added to all URLs
PUBLIC_BASE_PATH=

# ============================================================================
# AUTHENTICATION
# ============================================================================
# Directus API token (same as backend DIRECTUS_API_TOKEN)
# This will be set by the setup script
DIRECTUS_TOKEN=changeme_directus_api_token

# Optional: Directus email/password (alternative to token)
# DIRECTUS_EMAIL=admin@example.com
# DIRECTUS_PASSWORD=password

# Session secret (generate with: openssl rand -hex 32)
# This will be auto-generated by the setup script
SESSION_SECRET=changeme_session_secret_min_32_chars

# ============================================================================
# ENVIRONMENT
# ============================================================================
# Should match root .env and backend .env
NODE_ENV=development  # or production
PORT=4321

# Application name (displayed in UI)
PUBLIC_APP_NAME="Office Automation"

# ============================================================================
# FILE PROCESSING
# ============================================================================
# Maximum file size in bytes (default: 10MB)
MAX_FILE_SIZE=10485760

# Allowed file types (comma-separated MIME types)
ALLOWED_FILE_TYPES=text/csv,application/vnd.ms-excel,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,application/pdf

# File retention period in days
FILE_RETENTION_DAYS=30

# ============================================================================
# EMAIL NOTIFICATIONS (Optional)
# ============================================================================
# Configure SMTP for email notifications
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@example.com

# For development, use MailHog (included in Docker setup):
# SMTP_HOST=mailhog
# SMTP_PORT=1025

# ============================================================================
# ERROR TRACKING (Optional)
# ============================================================================
# Sentry for error tracking and monitoring
SENTRY_AUTH_TOKEN=

# ============================================================================
# NOTES
# ============================================================================
# Docker vs Local Development:
# 
# When running in Docker:
#   - DIRECTUS_URL=http://directus:8055 (service name)
#   - ORCHESTRATION_API_URL=http://orchestration-api:3001 (service name)
#   - PUBLIC_DOMAIN=dev-dejtoai.local (or your production domain)
#
# When running locally (without Docker):
#   - DIRECTUS_URL=http://localhost:8055 (direct port)
#   - ORCHESTRATION_API_URL=http://localhost:3001 (direct port)
#   - PUBLIC_DOMAIN=localhost:4321
#
# The setup script (make setup-dev) will automatically configure these
# values based on your environment selection.
#
# To generate secure secrets manually:
#   openssl rand -hex 32  # For SESSION_SECRET
