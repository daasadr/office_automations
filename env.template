# Office Automation - Root Environment Configuration
# This file contains only Traefik and general Docker setup variables
# Component-specific variables should be in backend/.env or frontend/.env
#
# Command: cp env.template .env

# ============================================================================
# PROJECT CONFIGURATION
# ============================================================================
# Note: PROJECT_PREFIX is no longer used. Container names are now service names only.

# ============================================================================
# TRAEFIK REVERSE PROXY
# ============================================================================
# Domain configuration
# Production: dejtoai.cz
# Development: dev-dejtoai.local (add to /etc/hosts: 127.0.0.1 dev-dejtoai.local)
DOMAIN=dev-dejtoai.local

# Email for Let's Encrypt SSL certificates
ACME_EMAIL=admin@dejtoai.cz

# ============================================================================
# ENVIRONMENT
# ============================================================================
# Environment mode (affects all services)
NODE_ENV=development  # or production

# ============================================================================
# NOTES
# ============================================================================
# Backend services configuration: See backend/.env
# - PostgreSQL (database)
# - KeyDB (cache)
# - MinIO (object storage)
# - Directus (CMS)
# - Orchestration API
# - Gemini API key
#
# Frontend configuration: See frontend/.env
# - Session secrets
# - Public URLs
#
# IMPORTANT: Docker Compose requires all variables used in ${VAR} substitutions
# to be present in the root .env file. After creating backend/.env and
# frontend/.env, you must copy their variables here for Docker Compose to work.
#
# To set up environments:
# 1. cp env.template .env
# 2. cp backend/env.template backend/.env
# 3. cp frontend/env.template frontend/.env
# 4. Update values in backend/.env and frontend/.env
# 5. Append backend variables to root .env:
#    grep -E "^[A-Z_]+=.*$" backend/.env | grep -v "^#" >> .env

