---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { FileSpreadsheet, ArrowRight, FileCheck, Download } from "lucide-react";
import { buttonVariants } from "@/components/ui/button";
import { cn } from "@/lib/utils";
---

<style>
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    opacity: 0;
    animation: fadeInUp 0.6s ease-out forwards;
  }

  .title {
    animation-delay: 0.2s;
  }

  .description {
    animation-delay: 0.4s;
  }

  .step-1 {
    animation-delay: 0.6s;
  }

  .step-2 {
    animation-delay: 0.8s;
  }

  .step-3 {
    animation-delay: 1s;
  }

  .cta-button {
    animation-delay: 1.2s;
  }

  @media (prefers-reduced-motion: reduce) {
    .animate-fade-in {
      animation: none;
      opacity: 1;
    }
  }
</style>

<BaseLayout title="Zpracování dokumentů průběžné evidence odpadů do Excelu" currentStep="welcome">
  <div class="container mx-auto px-4 py-16">
    <div class="max-w-3xl mx-auto text-center space-y-8">
      <h1 class="animate-fade-in title text-5xl font-bold tracking-tight mb-4 bg-gradient-to-r from-primary via-orange-500 to-amber-500 bg-clip-text text-transparent">
        Zpracování PDF dokumentů průběžné evidence odpadů do Excelu
      </h1>
      <p class="animate-fade-in description text-xl text-muted-foreground">
        Kontroluje nahraný soubor, zda obsahuje potřebné údaje <br />a převede PDF do Excel souboru. 
        <br />
        <br />
        Nahrajte soubory a aplikace se postará o zbytek.
      </p>

      <div class="grid md:grid-cols-3 gap-8 mt-16">
        <div class="animate-fade-in step-1 flex flex-col items-center space-y-4 p-6 rounded-lg border bg-card text-card-foreground shadow-sm hover:shadow-md transition-shadow">
          <div class="p-3 rounded-full bg-primary/10 text-primary">
            <FileSpreadsheet className="w-8 h-8" />
          </div>
          <h3 class="text-lg font-semibold">1. Nahrání souboru</h3>
          <p class="text-sm text-muted-foreground text-center">
            Nahrajte svůj PDF soubor
          </p>
        </div>

        <div class="animate-fade-in step-2 flex flex-col items-center space-y-4 p-6 rounded-lg border bg-card text-card-foreground shadow-sm hover:shadow-md transition-shadow">
          <div class="p-3 rounded-full bg-primary/10 text-primary">
            <FileCheck className="w-8 h-8" />
          </div>
          <h3 class="text-lg font-semibold">2. Zpracování</h3>
          <p class="text-sm text-muted-foreground text-center">
            Systém kontroluje nahraný soubor, zda obsahuje potřebné údaje a vygeneruje Excel soubor.
          </p>
        </div>

        <div class="animate-fade-in step-3 flex flex-col items-center space-y-4 p-6 rounded-lg border bg-card text-card-foreground shadow-sm hover:shadow-md transition-shadow">
          <div class="p-3 rounded-full bg-primary/10 text-primary">
            <Download className="w-8 h-8" />
          </div>
          <h3 class="text-lg font-semibold">3. Stažení</h3>
          <p class="text-sm text-muted-foreground text-center">
            Získejte zpracovaný soubor připravený ke stažení během chvilky.
          </p>
        </div>
      </div>

      <div class="mt-16">
        <a
          href="/upload"
          class={cn(
            buttonVariants({ size: "lg" }),
            "animate-fade-in cta-button text-lg rounded-full shadow-lg hover:shadow-xl hover:scale-105 transition-all"
          )}
        >
          Začít
          <ArrowRight className="w-5 h-5" />
        </a>
      </div>
    </div>
  </div>
</BaseLayout>